User-agent: *
Allow: /

# Staging sandboxes — noindex meta is on the pages too, but a crawler can
# reach the path without ever parsing the meta, so disallow it here as well.
Disallow: /v2/
Disallow: /v3/

Sitemap: https://liav-chanoch.github.io/morechain/sitemap.xml
