# MoreChain is a fully static site with no build step and no runtime secrets.
# This file exists as the template for anything added later (analytics id,
# form endpoint, etc). Copy to `.env` — `.env` is gitignored.
#
# MORECHAIN_FORM_ENDPOINT=
# MORECHAIN_ANALYTICS_ID=
